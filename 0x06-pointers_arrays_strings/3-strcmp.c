#include "main.h"
#include <string.h>
/**
 * _strcmp - compar two strings
 * @s1: the first str
 * @s2: the second str
 * Return:the pointer to dest.
 */
int _strcmp(char *s1, char *s2)
{
return (strcmp(s1, s2));
}
